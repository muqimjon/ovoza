# Telegram lead notifications (functions/api/lead.ts) — set these in Cloudflare Pages → Settings → Environment variables
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: override the canonical base URL used by the sitemap generator (tools/gen-seo.mjs)
# APP_BASE_URL=https://idea-up.uz
