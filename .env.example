# Telegram lead notifications (api/lead.ts) — set these in Vercel Project Settings → Environment Variables
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: override the canonical base URL used by the sitemap generator (tools/gen-seo.mjs)
# APP_BASE_URL=https://ovoza.vercel.app
